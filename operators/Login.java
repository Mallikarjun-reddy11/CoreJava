package operators;

public class Login {
	public static void main(String[]args) {
		boolean validUser = true;
		boolean validPassword = false;
		boolean result = (validUser && validPassword)?(true):(false);
		System.out.print(result);
	}

}
